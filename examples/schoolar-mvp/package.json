{
  "name": "schoolar-mvp",
  "private": true,
  "type": "module",
  "description": "Minimal vertical slice (tenant + student) — measurable target for task T1.",
  "scripts": {
    "test": "node --import tsx --test \"*.test.ts\"",
    "test:t1": "node --import tsx --test student.test.ts",
    "test:t3": "node --import tsx --test tenant.test.ts"
  }
}
